version: "3"

services:
  client:
    ports:
      - "3000:3000"
    entrypoint: npx serve -s build/ -l 3000
    environment:
      NODE_ENV: production
    build:
      args:
        NODE_ENV: production
    restart: unless-stopped
